#!/bin/bash
cd `dirname $0`
. ../common/get-directories

# This will break down for half-hour timezone offsets
LOCALSECONDS=$(expr `date +%z` / 100 \* 60 \* 60 + `date +%s`)
echo -n $FROM;echo -n $BASE;echo $LOCALSECONDS\|$LOCALSECONDS


